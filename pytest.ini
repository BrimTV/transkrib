[pytest]
testpaths = tests
markers =
    slow: медленные тесты (модели, сквозные прогоны) — не гоняются в CI по умолчанию;
        `pytest -q -m "not slow"` их пропускает, полный прогон — по расписанию/вручную.
