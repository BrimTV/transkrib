# ЧИТАЙ.txt в архиве для Windows-пользователей: должен долетать до диска ровно
# в тех байтах, в которых записан — UTF-8 с BOM (для Блокнота) и CRLF (стиль
# Windows). "-text" отключает любую нормализацию переводов строк со стороны git
# независимо от core.autocrlf у того, кто клонирует репозиторий.
build/README-windows.txt -text
